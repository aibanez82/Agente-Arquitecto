# Bug #7 — Django no escribe estatus_pago = PAGADO al confirmar pago

**Sistema:** Django · **Estado:** 🟠 Alto (workaround activo en Dashboard — ver CLAUDE.md)

## Fila de la tabla original

| 7 | Django no escribe `estatus_pago = 'PAGADO'` al confirmar pago — solo dispara webhook a n8n | Django | 🟠 Alto |

## Workaround activo en Dashboard

El workaround vive directo en CLAUDE.md (sección "Bugs — índice", operativo y activo) — no se duplica aquí.

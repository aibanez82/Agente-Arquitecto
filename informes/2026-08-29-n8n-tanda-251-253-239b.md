# Informe único de la tanda — #251, #253, #239 B (con sus dos adendas)

**De:** Agente n8n · **Fecha:** 2026-08-29 tarde · Órdenes: handoffs del #251, #253, #239 B + adendas 1 y 2, y el canal directo del Arquitecto.

## Estado de los dos entornos al cierre

- **STG** `dNqtM20ij6ecZYAX`: **`58ac2738`**, 270 nodos, 57 postgres estrictos.
- **PROD** `BtOaZm7WlZT-24V7hqCnF`: **`2f40b5ef`**, 255 nodos, 51 postgres (solo entró el #251).

## #251 — mención no es volición (STG y PROD)

Frontera del `Discount Intent Classifier`: `COVERAGE_DOWNGRADE` exige volición explícita («NOMBRARLA NO BASTA»); el desempate `no_match` vale también para la extensión; signal y traductor intactos.
- **STG** (`b3b030ad`): batería §4 **10/10** (5 informativas → `no_match`, 5 volitivas → `COVERAGE_DOWNGRADE`) + protección medida (`está muy caro` sigue `PRICE_OBJECTION`). Reproducible: `scripts/test-251-bateria-clasificador.py`.
- **PROD** (`2f40b5ef`): promovido bajo la **autorización permanente** (CLAUDE.md del Arquitecto `84507d9`, **verificada en el fichero** antes de tocar; tres condiciones auditadas). Aceptación estructural 7/7 (un campo, fila 5 = exactamente 1 nodo difiere). Validación en STG: exec `22145` re-verificada por el ejecutor. **Pendiente en vivo en PROD**: «¿y la limitada?» y «está caro» desde el teléfono de Alberto.

## #253 — el VIN de la guarda no se pierde (STG `396cbfb8`)

Dos escrituras en la cadena de la guarda: `Persist Guard VIN` (merge DENTRO de `grupo2.serie`, lock por teléfono, gate `vin_registrado`, `grupo1` intacto) e `Insert Guard Turn History` (el turno al historial **tras el settle de envío**). Auditoría §4: la guarda captura hoy **solo el VIN**, con su ámbito de búsqueda declarado. **Pendiente §5 en vivo**: dar el VIN y comprobar que la captura NO vuelve a pedirlo (la fila que acredita).

## #239 B — el sumidero no muere mudo (STG, con las dos adendas)

- **Router por datos** (no por arista) en `Discount Reply Terminal`, ratificado en el dictamen `26e859e`: silencio legítimo auditado / copy vencida / copy Django verbatim / carril normal. Claim propio `d239.terminal`. La reutilización de la cadena de la guarda se descartó **con datos** (exec 17454). Contradicción §3↔§5.5 resuelta a favor del claim (dictamen §1).
- **Adenda 2** (`58ac2738`): la rama `copy` reserva con la **sesión vigente** del teléfono (`conversation_control_v1`, la vista de la valla — la valla no se tocó); cazada además la referencia latente a `Session Resolution`, que no corre en el camino corto del botón.
- **Aceptación en vivo — el caso real, ejercitado por inyección firmada** (exec **`22173`**, no un caso cómodo): botón de oferta **caducada** (`b2bd13cb`, sesión `waq_2288` cerrada) con la sesión activa del teléfono en `waq_2290` — la misma divergencia que tumbó la `22141` con `sesion_no_elegible`. Resultado: `Resolve Terminal Session` → `waq_2290`; `Claim` → `puede_intentar=true, rechazo=null`; **un solo** `Send Terminal Reply` → `wamid` de Meta real; dispatch `sent`. El cliente recibió: «Esa oferta ya venció — tienen una vigencia de 30 minutos. ¿Quieres que revise si sigue disponible para ti?». La técnica: POST firmado (HMAC con el app secret) al webhook del trigger — el mismo camino que un botón real de Meta, sin tocar la base. Script: `scripts/239b/inyectar-boton-caducado-stg.py`.
- **No ejercitadas y declaradas**: el camino feliz del router (una oferta que sí sale → un mensaje) y las demás entradas del sumidero — provocarlas sin artificio no estaba a mi alcance esta tanda. Y la foto con pie durante descuento (exec 22064) es del #240, fuera de este arreglo por dictamen.

## Deuda #238

Postgres estrictos de STG 51 → 57 en esta tanda: 3 del router del #239 B, 2 del #253, 1 del resolutor de la adenda 2 — todas anotadas en el issue con su motivo. PROD sigue en 51.

## Pila de PROD pendiente de aceptación conversacional de Alberto

#251 (limitada/caro), #244 pieza 1 (ASK_VIN en landing), caption de imagen, liga de pago (#207). El #250 (emisión rota) sigue sin dictamen. Monitor de handoffs v5 (gh api, ceguera acotada) tras la caída del continuo v2 — autopsia en el chat de la sesión.

— Agente n8n

# Informe — noche 28-29 ago: #232 arreglado, #243+#244 puestos, cola de PROD armada

**De:** Agente n8n · **Fecha:** 2026-08-29 ~03:4x UTC
**Órdenes:** ALTO del #232; handoff #243+#244 («vamos a por los fixes»). El #245 y el #239 B quedan en cola, sin empezar.

## STG — tres deploys esta noche, en orden

| versionId | Qué entró |
|---|---|
| `6dfd7caa` (246→248) | **#232 §3**: `Buffer Load` con `phone eq` + `state neq done`, `returnAll`, orden `received_at DESC`; `mine` ausente = **error ruidoso**; purga de `done` >24 h tras `Buffer Mark Done` (`onError: continue`) |
| `4c06511b` (248→259) | **#243+#244**: guarda determinista en `IF Continue Normal?`→true (lee `qualitas_discountapplication` de la sesión; con aplicación viva entrega el `processing_copy` de Django **verbatim** por cadena outbound propia `d243.p2.guard`; copy vacío = error, no improvisación); `pending_data` + VIN de 17 en el texto → `provide_required_data`; `pending_data` sin VIN → petición determinista del VIN; **pieza 1 #244**: `Validate Django Resolution` sustituye el copy prometedor por la petición del VIN cuando `next_action === 'await_required_data'`. Incluye **`Persist Django Resolution` a forma array** (tu dictamen del #239 A, «en STG primero») — prerrequisito para que los copies con comas no corrompan el insert |

Exports `_stg` sincronizados en `origin/stg` tras cada deploy. Scripts en `chore/scripts-noche-232-243-244` (`7512b61`), sin mergear.

## Decisiones de diseño que te debo declarar (dictamen si procede)

1. **La guarda del #243 NO re-entra al carril clasificado** (opción a del handoff): re-consultar availability con intent `no_match` atraviesa `n8n_discount_phase2_availability_settle` y `IF Create Discount Offer?` con semántica no verificada — riesgo de oferta espuria clase #205 y de bucle. Fui a la opción b con Postgres como fuente del estado (los 46 nodos postgres existentes son el patrón vigente; anotado el roce con el rumbo del #238).
2. **Estados que bloquean**: `queued/processing/awaiting_conversation/ready/uncertain` → processing_copy; `pending_data` → pedir el dato (tu corrección a la duda: la pelota es del cliente). Sin `LIVE_SOURCE_STATES` tal cual.
3. **Copy de la petición de VIN**: determinista, compuesto en n8n (el handoff #244 prohíbe repetir el de Django en ese caso). También se usa como respuesta cuando `provide_required_data` rechaza el VIN (`invalid_vin`) o devuelve forma imprevista — el motivo queda en el output del nodo para trazar.
4. **Fallo del check de la guarda** (Postgres caído): `alwaysOutputData` + fila vacía → sigue por el carril normal (statu quo de hoy) en vez de enmudecer el turno. Elegido contra la alternativa de bloquear: un apagón de la guarda no puede callar al bot entero.

## Aceptación pendiente — necesita el teléfono de Alberto (se lo pedí en la sesión)

Tabla sucia preparada (su número: 62 filas, 55 sembradas `seed_232`). Su sesión actual (`waq_2284...`) es la de la **aplicación 236** en `pending_data`, así que una sola pasada lo ejercita todo: ráfaga → una única respuesta (pedirá el VIN) [#232-1/2]; VIN → `provide_required_data`, la 236 sale de `pending_data` [#244-1/2 y su aceptación 4]; mensaje suelto durante el procesamiento → processing_copy, sin precio viejo [#243-1/2]; tras la entrega → conversación normal [#243-3]. `mine` ausente ya probado en réplica (error visible). Ids de ejecución al cerrar la pasada.

## Gotchas de instancia descubiertos (van a docs/gotchas-n8n.md con los scripts)

- `$now.minus(...)` devuelve **Invalid DateTime** en el sandbox de expresiones (3 sondas); `$now.toISO()` sí funciona. Cutoffs: calcularlos en un nodo Code con `Date`.
- La API pública no admite DELETE/PATCH de filas de Data Tables (405) — mutaciones vía workflow temporal con nodo `dataTable`.
- La condición `lt` sobre la columna de sistema `createdAt` exige un DateTime que no sobrevive la frontera del runner; sobre columnas string (`received_at`) funciona con strings del mismo formato.

## PROD — sin tocar desde el #207

`#239 A` con candidato en seco PASS (cuadre $8/$7/$5/$7/$5, huellas idénticas contra STG) y tu §4 ya lo da por verde en STG (exec `21057`); espera la orden de arranque de Alberto en el canal directo, como el #207. El `#232` a PROD queda derogado por tu ALTO hasta que la aceptación de arriba pase.

— Agente n8n

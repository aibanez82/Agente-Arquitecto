# Informe `#282b` — el settle ya devuelve el wamid que acaba de escribir (STG). Listo para el E2E de Juan

**De:** Agente n8n · **Para:** Arquitecto · 2 sep 2026
Handoff `0b08a0c`. Aplicado tras cerrar la aceptación 4/4 del `#293` en PROD (tu condición).

## 1 · Paso 0: tu hipótesis, CONFIRMADA con medición (banco, transacción con ROLLBACK)

| medición | resultado |
|---|---|
| Sentencia del nodo TAL CUAL, primer settle | `{aplicado: true, provider_message_id: None}` |
| La fila tras esa MISMA sentencia | `provider_message_id = 'wamid.BENCH282B'` |
| Settle REPETIDO (sentencia vieja) | devuelve el wamid — instantánea nueva |

El `JOIN` del `SELECT` exterior lee la instantánea del comienzo de la sentencia, anterior al `UPDATE`
de `n8n_outbound_settle` (plpgsql volátil). **Solo el PRIMER settle miente** — por eso la fila de
Juan está bien y su respuesta llevó `null`: son el mismo hecho visto en dos instantes.

## 2 · El arreglo (tu opción a) y su estado

`COALESCE(NULLIF(NULLIF($2,''),'null'), dispatch.provider_message_id)` en el `SELECT` exterior — la
sentencia devuelve lo que acaba de escribir, sin releerlo. `$2` reutilizado: **binds intactos**;
**`n8n_outbound_settle` sin tocar** (compartida). Aplicado a STG:

| | |
|---|---|
| Retomar Conversacion_stg | `nYRaRzU83qDLuEWI`, **`02116e87-85d3-449d-b898-bc5600ef72b1`**, 27 nodos, active |
| Diff | **solo la `query` de `Settle Checkpoint Sent`**; binds, connections y todo lo demás byte a byte |
| Espejo | sincronizado en `origin/stg`; scripts en `scripts/282b/` |

## 3 · Aceptación — medida sobre el SQL VIVO (extraído del nodo desplegado, no de mi script)

| # | Criterio | Veredicto |
|---|---|---|
| 1 | La sentencia devuelve el wamid que acaba de escribir | **PASS** — `provider_message_id = 'wamid.BENCH282B.VIVO'` en el primer settle, igual al de la fila |
| 2 | La respuesta HTTP lleva `provider_message_id` no nulo | **PASS por composición medida**: salida del settle vivo (medida) → `Build Checkpoint Success Response` literal (`settled.provider_message_id \|\| null` → el wamid) → `responseMode: lastNode`. El disparo HTTP real de punta a punta lo dará el E2E de Juan — no fabriqué un followup ni mandé un WhatsApp al teléfono de Juan para simularlo |
| 3 | La fila sigue correcta | **PASS** — `('sent', wamid, attempts 0)`, sin cambios de forma |
| 4 | El origen `Send Discount Offer` | **NO EJERCITADO — cubierto por construcción.** Misma forma de salida (`{messages:[{id}]}` del mismo endpoint del Graph API; el httpRequest sin `fullResponse` entrega el body, medido en exec 28110 para el origen texto), mismo bind, mismo arreglo. Evidencia de por qué no es medible hoy: **0 filas** de oferta con conector `retomar_checkpoint_v06` — nadie lo ha ejecutado jamás. **Su primera ejecución real será el E2E de Juan**; si falla ahí, es de esta familia |
| 5 | Settle repetido idempotente | **PASS** — `aplicado=false, rechazo=null`, 1 fila, y además ahora devuelve el wamid también en el repe |

## 4 · Qué queda

- **Juan puede repetir su E2E desde `quote_sent:1`** — el aviso es tuyo (§6). Con el arreglo, el
  primer settle devolverá el wamid, Django acreditará `quote_sent:1` y la cadena podrá llegar a la
  oferta — que además ejercitará por primera vez el origen del criterio 4.
- **PROD**: dentro de la autorización permanente con este PASS, pero lo ordenas tú con este informe
  (§6). La misma pieza aplica: el nodo de PROD tiene la query vieja idéntica (el script re-verifica
  el literal y PARA si divergió).

— Agente n8n
